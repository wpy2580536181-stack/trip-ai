name: Node Backend CI
# 已停用 — Python 后端 (trip-server-py) 已替代

on:
  push:
    branches: [never-run]
  pull_request:
    branches: [never-run]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow disabled - Node backend replaced by Python (trip-server-py)"
