# 2026-07-01 工作日志

## 为项目绘制架构图、Agent 时序图、RAG 流程图

通过 diagram-builder skill，通读项目核心代码后用 show_widget 内联 SVG 绘制三张图：

1. **项目分层架构图** — 六层：前端(trip-front Vue3) → 接入层(Express5+中间件+SSE) → 业务服务层 → Agent层(LangGraph, purple) → RAG检索层(knowledgeService, purple) → 基础设施层(MySQL+ChromaDB+Redis / DeepSeek+高德MCP+Unsplash+HF)。
2. **Agent 对话时序图** — 基于 chatGraph 执行流，6 条生命线(用户/前端/后端/Agent/工具LLM/存储)，17 条消息，分四阶段(会话初始化/路由并行检索/流式生成/收尾持久化)，SSE 事件用 teal 色区分。
3. **RAG 流程图** — searchSpots 端到端：输入 → rewriteQuery(本地关键词~50ms) → 三路并行召回(Chroma向量top20 / MySQL FULLTEXT top10 / MySQL rating top10) → RRF融合(K=60) → 高分跳过判断(>0.15) → Cross-Encoder重排(bge-reranker-base) → 格式化输出。

### 关键代码位置（供后续参考）
- Agent 编排：`trip-server/src/services/agent/agentEngine.ts`、`plannerGraph.ts`、`chatGraph.ts`
- Agent 节点：`nodes/{router,research,planner,chatPlanner,validate}.ts`
- RAG 核心：`trip-server/src/services/knowledgeService.ts`（searchSpots 三路召回+RRF+重排）
- 重排序模型：`trip-server/src/services/reranker.ts`（bge-reranker-base）
- Embedding：`trip-server/src/config/embeddings.ts`（bge-small-zh-v1.5, 512维）
- 向量库：`trip-server/src/config/chroma.ts`（travel_spots collection, cosine）

## SVG 图表导出 (21:41)

用户要求把之前绘制的内联 SVG 图表导出为独立文件。已将四张图写入 `docs/diagrams/` 目录：
1. `architecture-overview.svg` - 项目分层架构图（六层：前端/接入/业务服务/AI核心/数据/外部服务）
2. `agent-sequence.svg` - Agent 对话任务时序图（chatGraph 场景，含 SSE 事件流）
3. `rag-pipeline.svg` - RAG 检索增强生成流程图（三路并行召回 + RRF 融合 + 重排序）
4. `langchain-vs-langgraph.svg` - LangChain 与 LangGraph 分工对比图

所有图表均为深色主题，基于项目实际代码逻辑绘制。可用浏览器直接打开查看。
